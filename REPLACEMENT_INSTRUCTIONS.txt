BANGLASANGBAD PREMIUM AUTO-PUBLISH V4

Replace:
  automation/auto_publish.py

with the supplied auto_publish.py.

Keep these GitHub Secrets exactly where they are:
  META_PAGE_ID
  META_PAGE_ACCESS_TOKEN
  INSTAGRAM_BUSINESS_ACCOUNT_ID

No token or ID is hard-coded into this file.

Important fixes:
1. Instagram now returns status="posted" when Meta returns a published media ID.
   Your previous log showed an Instagram ID but the old code marked it failed.
2. Facebook never attempts comment publishing.
3. Facebook uses the Page /photos endpoint with the Page access token.
4. Detailed START/OK/FAILED logging is included.
5. Failed platform entries remain retryable.
6. Existing Google Sheet controls are preserved.
7. Facebook + Instagram remain the automatic defaults.
8. X/Threads/YouTube stay disabled unless explicitly enabled.

After replacement:
  - Commit changes.
  - Run the Social Auto Publish workflow.
  - Set LIMIT=1.
  - Use DRY_RUN=false.

IMPORTANT ABOUT THE FACEBOOK 403:
If the run still reports:
  "(#200) The permission(s) publish_actions are not available..."

then the remaining problem is NOT a missing Python function. It means the Meta credential/app configuration being used for the Page request is being rejected for that operation. Do not put a token into this Python file and do not manually mark an article as posted.

The new Instagram logic should no longer report "Platform publish did not complete successfully" when the API has actually returned a published media ID.
